# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Local environment — holds live Supabase credentials and the service-role key.
# Never commit these; deploys read them from the host's secret store instead.
.env
.env.*
!.env.example

# Supabase CLI link state (contains the linked project ref and pooler config)
supabase/.temp/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

test-results
playwright-report

# Lighthouse / CWV reports
lighthouse-report
