node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts

# Generated files. Formatting these makes `npm run format` fight whatever
# regenerates them: the next `supabase gen types` / `images:manifest` run would
# undo it and reopen the same lint errors.
src/integrations/supabase/types.ts
src/lib/image-variants.ts

# Auto-generated by the MCP Vite plugin, which rewrites them verbatim (banner
# included) on every dev start — formatting them only reopens the same errors.
src/routes/mcp.ts
src/routes/\[.mcp\]/
src/routes/\[.well-known\]/
